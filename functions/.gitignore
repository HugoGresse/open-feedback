## Compiled JavaScript files
lib/**/*.js
lib/**/*.json
lib/**/*.js.map

# Typescript v1 declaration files
typings/

node_modules/

# Env / config files may hold secrets. Ignore them all (including the
# per-project `.env.<project-id>` and emulator `.env*.local`) except the
# tracked placeholder `.env.default`.
.env
.env.*
!.env.default
