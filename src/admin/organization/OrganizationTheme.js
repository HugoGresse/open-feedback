import React from 'react'

export const OrganizationTheme = () => {
    return (
        <div>
            <div>
                Organization are work in progress and has been merged to prevent
                review overflow. Only organizations users are completed for now.
            </div>
        </div>
    )
}
